[Unit]
Description=EntityBase API
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/entitybase
EnvironmentFile=/opt/entitybase/.env
ExecStart=/usr/bin/python3 -m entitybase serve
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
